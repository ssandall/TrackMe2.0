{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "api.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon -r dotenv/config api.js",
    "deploy": "now remove API -y && now -p && now alias",
    "doc": "apidoc -o public/generated-docs/ -e node_modules/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^6.0.0",
    "express": "^4.16.3",
    "mongoose": "^5.2.14"
  },
  "devDependencies": {
    "apidoc": "^0.17.6",
    "nodemon": "^1.18.4",
    "now": "^11.4.4"
  }
}
