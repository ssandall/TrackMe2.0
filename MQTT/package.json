{
  "name": "mqtt-api",
  "version": "1.0.0",
  "description": "MQTT server",
  "main": "mqtt.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon -r dotenv/config mqtt.js",
    "deploy": "now remove MQTT-API -y && now -p && now alias"
  },
  "author": "Suriya Sandall",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "mqtt": "^2.18.8"
  },
  "devDependencies": {
    "dotenv": "^6.0.0",
    "nodemon": "^1.18.4",
    "now": "^11.4.5"
  }
}
